{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "docker build -t tiinusen/maps4fs -f Dockerfile_replacement .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}